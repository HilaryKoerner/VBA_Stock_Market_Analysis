'Create a script that will loop through all the stocks for one year 
'output the following information
    '(1)the ticker symbol
    '(2)yearly change from opening price at the beginning of a given year 
    'to the closing price at the end of that year
    '(3)the percent change from opening price at the beginning of a given yearl
    'to the closing price at the end of that year
    '(4)the total stock volumn of the stock
    'note: have conditional formating that will highlight postive change
    'in green and negative change in red

Sub VBA_Homework()
'What needs to be defined with DIM?



'label the additional columns (ticker, yearly change, percent change, and total stock volumne)
'need to name the cells on each sheet
thisworkbook.sheets.range(I2).value=Ticker
thisworkbook.sheets.range(J2).value=Yearly Change
thisworkbook.sheets.range(k2).value=Percent Change
thisworkbook.sheets.range(L2).value=Total Stock Value



